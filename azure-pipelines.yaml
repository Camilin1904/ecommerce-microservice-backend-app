trigger:
- master

jobs:

  - job: Validate
    pool: server
    steps:
      - task: ManualValidation@1
        inputs:
          notifyUsers: |
            1105362068@u.icesi.edu.co
          approvers: |
            1105362068@u.icesi.edu.co


  - job: Deploy_to_prod
    pool:
      vmImage: ubuntu-latest

    steps:

      - task: Maven@3
        inputs:
          mavenPomFile: 'pom.xml'
          mavenOptions: '-Xmx3072m'
          javaHomeOption: 'JDKVersion'
          jdkVersionOption: '11'
          jdkArchitectureOption: 'x64'
          publishJUnitResults: true
          testResultsFiles: '**/surefire-reports/TEST-*.xml'
          goals: 'package'


      - task: Docker@2
        inputs:
          containerRegistry: 'dockerHub'
          command: 'login'


      - task: DockerCompose@1
        inputs:
          containerregistrytype: 'Container Registry'
          dockerRegistryEndpoint: 'dockerHub'
          dockerComposeFile: 'compose.yml'
          projectName: 'ecommerce'
          action: 'Build services'
          includeLatestTag: true

      - task: DockerCompose@1
        inputs:
          containerregistrytype: 'Container Registry'
          dockerRegistryEndpoint: 'dockerHub'
          dockerComposeFile: 'compose.yml'
          projectName: 'ecommerce'
          action: 'Push services'
          includeLatestTag: true

          
      - task: KubectlInstaller@0
        inputs:
          kubectlVersion: 'latest'

      - task: KubernetesManifest@1
        inputs:
          action: 'deploy'
          connectionType: 'azureResourceManager'
          azureSubscriptionConnection: 'ecommerce-connection-manual'
          azureResourceGroup: 'rg-ecommerce-prod'
          kubernetesCluster: 'aks-ecommerce-prod'
          useClusterAdmin: true
          manifests: 'k8s/*.yaml'
      
      - task: CmdLine@2
        inputs:
          script: 'sleep 30'

      - task: KubernetesManifest@1
        inputs:
          action: 'deploy'
          connectionType: 'azureResourceManager'
          azureSubscriptionConnection: 'ecommerce-connection-manual'
          azureResourceGroup: 'rg-ecommerce-prod'
          kubernetesCluster: 'aks-ecommerce-prod'
          useClusterAdmin: true
          manifests: 'k8s/e2e/e2e.yaml'
      
      - task: CmdLine@2
        inputs:
          script: 'sleep 30'
          
      - task: Kubernetes@1
        inputs:
          connectionType: 'Azure Resource Manager'
          azureSubscriptionEndpoint: 'ecommerce-connection-manual'
          azureResourceGroup: 'rg-ecommerce-prod'
          useClusterAdmin: true
          kubernetesCluster: 'aks-ecommerce-prod'
          command: 'logs'
          arguments: 'job/newman-job'
          secretType: 'dockerRegistry'
          containerRegistryType: 'Azure Container Registry' 




